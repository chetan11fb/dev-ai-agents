# Angular UI + API Integration Prompt

Inspect the actual Angular version and existing component/service/state/routing patterns.

Implement the user journey with typed API contracts, loading/error/empty states, validation, responsive behavior, keyboard accessibility and tests. Verify the backend contract before inventing new endpoints or DTOs. Keep UI and API changes traceable to the story.
